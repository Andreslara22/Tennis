# deps
node_modules
package-lock.json

# build
dist
dist-ssr
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# capacitor — el proyecto android/ SÍ se versiona (firma, permisos, gradle),
# pero no sus artefactos de build ni configuración local:
android/.gradle/
android/.idea/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
android/.kotlin/

# iOS (aún no generado)
ios

# 🔐 FIRMA — NUNCA subir la clave ni sus contraseñas
*.keystore
*.jks
keystore.properties

# env & secrets
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
