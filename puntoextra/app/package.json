{
  "name": "puntoextra",
  "private": true,
  "version": "1.2.0",
  "description": "Punto Extra — el tutor de toda la familia. Membresía educativa familiar por WhatsApp + app.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sync": "npm run build && npx cap sync android",
    "android": "npm run sync && npx cap open android",
    "android:apk": "npm run sync && cd android && ./gradlew assembleRelease",
    "android:release": "npm run sync && cd android && ./gradlew bundleRelease",
    "assets": "node scripts/generate-assets.mjs",
    "portal": "npm run build && rm -rf ../landing/app && cp -r dist ../landing/app"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "sharp": "^0.33.4",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}